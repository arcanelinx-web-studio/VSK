(()=>{const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const items=$$('.acc-item');items.forEach((item,i)=>{const btn=$('.acc-trigger',item);btn.addEventListener('click',()=>{const open=item.classList.contains('is-open');items.forEach(x=>{x.classList.remove('is-open');$('.acc-trigger',x)?.setAttribute('aria-expanded','false')});if(!open){item.classList.add('is-open');btn.setAttribute('aria-expanded','true')}});if(i===0){item.classList.add('is-open');btn.setAttribute('aria-expanded','true')}});const preview=$('.hover-preview'),previewImg=$('.hover-preview img');$$('.project-row').forEach(row=>{row.addEventListener('pointerenter',()=>{if(matchMedia('(pointer:fine)').matches){previewImg.src=row.dataset.image;previewImg.alt=row.querySelector('strong')?.textContent||'VSK project';preview.classList.add('is-visible')}});row.addEventListener('pointermove',e=>{preview.style.left=`${Math.min(innerWidth-180,e.clientX+145)}px`;preview.style.top=`${Math.max(140,Math.min(innerHeight-140,e.clientY))}px`});row.addEventListener('pointerleave',()=>preview.classList.remove('is-visible'))});})();