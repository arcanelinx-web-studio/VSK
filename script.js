(()=>{const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const header=$('[data-header]');const progress=$('.scroll-progress span');const onScroll=()=>{const y=scrollY;header?.classList.toggle('is-scrolled',y>20);const max=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.width=(max>0?y/max*100:0)+'%'};addEventListener('scroll',onScroll,{passive:true});onScroll();const menuBtn=$('.menu-toggle'),nav=$('#site-nav');menuBtn?.addEventListener('click',()=>{const open=menuBtn.getAttribute('aria-expanded')==='true';menuBtn.setAttribute('aria-expanded',String(!open));nav?.classList.toggle('is-open',!open)});$$('#site-nav a').forEach(a=>a.addEventListener('click',()=>{nav?.classList.remove('is-open');menuBtn?.setAttribute('aria-expanded','false')}));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.14,rootMargin:'0px 0px -4%'});$$('[data-reveal], [data-process]').forEach(el=>io.observe(el));const counters=$$('[data-count]');const counterIO=new IntersectionObserver(entries=>entries.forEach(({target,isIntersecting})=>{if(!isIntersecting)return;const end=Number(target.dataset.count);if(reduce){target.textContent=end;counterIO.unobserve(target);return}const start=performance.now(),dur=1200;const tick=t=>{const p=Math.min(1,(t-start)/dur);target.textContent=Math.round(end*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);counterIO.unobserve(target)}),{threshold:.7});counters.forEach(el=>counterIO.observe(el));const tabs=$$('.solution-tab');const activateSolution=id=>{tabs.forEach(t=>{const active=t.dataset.solution===id;t.classList.toggle('is-active',active);t.setAttribute('aria-selected',String(active))});$$('[data-solution-image]').forEach(i=>i.classList.toggle('is-active',i.dataset.solutionImage===id));$$('[data-solution-copy]').forEach(i=>i.classList.toggle('is-active',i.dataset.solutionCopy===id));const map={build:'01',retrofit:'02',manufacture:'03'};const idx=$('[data-solution-index]');if(idx)idx.textContent=map[id]};tabs.forEach(t=>t.addEventListener('click',()=>activateSolution(t.dataset.solution)));if(!reduce){const parallax=$('[data-parallax]');addEventListener('pointermove',e=>{if(!parallax||innerWidth<900)return;const x=(e.clientX/innerWidth-.5)*14,y=(e.clientY/innerHeight-.5)*10;parallax.style.transform=`translate3d(${x}px,${y}px,0)`},{passive:true});$$('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();const x=(e.clientX-r.left-r.width/2)*.12,y=(e.clientY-r.top-r.height/2)*.12;btn.style.transform=`translate(${x}px,${y}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}const form=$('[data-enquiry-form]'),status=$('.form-status');const formData=()=>Object.fromEntries(new FormData(form).entries());const validate=()=>{if(!form.reportValidity())return false;const{phone}=formData();if(!/^\d{10}$/.test(phone)){status.textContent='Please enter a valid 10-digit mobile number.';$('input[name="phone"]')?.focus();return false}status.textContent='';return true};const compose=d=>`VSK Engineering Enquiry\n\nName: ${d.name}\nCompany: ${d.company||'-'}\nPhone: ${d.phone}\nEmail: ${d.email}\nRequirement: ${d.requirement}\n\nApplication / Requirement:\n${d.message}`;form?.addEventListener('submit',e=>{e.preventDefault();if(!validate())return;const d=formData();location.href=`mailto:vsk.electromech@gmail.com?subject=${encodeURIComponent('Website enquiry — '+d.requirement)}&body=${encodeURIComponent(compose(d))}`});$('[data-send="whatsapp"]')?.addEventListener('click',()=>{if(!validate())return;const d=formData();window.open(`https://wa.me/919880336714?text=${encodeURIComponent(compose(d))}`,'_blank','noopener')});const year=$('[data-year]');if(year)year.textContent=new Date().getFullYear()})();