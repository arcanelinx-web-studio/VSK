(()=>{const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,fine=matchMedia('(pointer:fine)').matches;if(fine)document.body.classList.add('has-pointer');const coord=$('.coordinate');if(coord&&fine)addEventListener('pointermove',e=>{coord.style.transform=`translate(${e.clientX-37}px,${e.clientY-37}px)`;const t=$('small',coord);if(t)t.textContent=`X ${String(e.clientX).padStart(4,'0')} / Y ${String(e.clientY).padStart(4,'0')}`},{passive:true});let lastY=0;const nav=$('.theatre-nav');addEventListener('scroll',()=>{const y=scrollY;nav?.classList.toggle('is-hidden',y>160&&y>lastY);lastY=y},{passive:true});const videos=$$('video[autoplay]');if(reduce)videos.forEach(v=>v.pause());else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.play().catch(()=>{});else e.target.pause()}),{threshold:.18});videos.forEach(v=>io.observe(v))}})();